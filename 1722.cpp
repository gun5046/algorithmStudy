#include <iostream>
#include <algorithm>
#include <utility>
#include <queue>
#include <math.h>

using namespace std;


int n;

void ordering1(int order, int depth) {
	
}

void ordering2(vector<int>v, int depth) {

}

int main() {
	cin >> n;

	int k = 0;

	cin >> k;
	if (k == 1) {
		int order;
		cin >> order;

		//ordering1(order);
	}
	else {
		vector<int>v;
		int num;
		for (int i = 0; i < num; i++) {
			cin >> num;
			v.push_back(num);
		}

		//ordering2(v);
	}
}