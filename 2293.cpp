#include <vector>
#include <iostream>
#include <algorithm>
#include <utility>

using namespace std;


int n;
int k;
vector<int>v;
int main() {
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		int num;
		cin >> num;
		v.push_back(num);
	}

}